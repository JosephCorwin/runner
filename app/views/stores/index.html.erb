<% provide(:title, "Store Index") %>
<%= will_paginate %>

<ul class="stores">
  <% @stores.each do |store| %>
    <li>
      <%= link_to store.name, store %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
